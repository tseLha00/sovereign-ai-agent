version: "3.9"

services:
  backend:
    build:
      context: ./backend
    container_name: sovereign-ai-backend
    environment:
      LLM_BACKEND: mock
      PYTHONPATH: /app/src
    ports:
      - "8000:8000"